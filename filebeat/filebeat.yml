filebeat.inputs:
  - type: container
    paths:
      - /var/lib/docker/containers/*/*.log

    processors:
      - add_docker_metadata: ~
      - drop_event:
          when:
            not:
              contains:
                container.name: "microcrm"

output.logstash:
  hosts: ["logstash:5044"]